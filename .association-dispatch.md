# 🏀 Association Theme — Task Dispatch

**From:** Marcus (Human Lead)
**Date:** 2026-02-18
**Status:** APPROVED — All agents begin work

---

## Context

The "Association" light theme redesign is approved. Mockups reviewed. A2 (Staff Engineer) reviewed and flagged 5 gaps — all answered by Agent D. No blockers remain.

**Design source:** `docs/mockups/` (3 PNGs) + `docs/mockups/generate-mockups.js` (token values)
**Full review thread:** `.design-review-request.md`

---

## Task Assignments by Agent

### 🔧 Agent A2 — Staff Engineer / Architect

You review, architect, and sign off. You do not implement.

| Task | What | Depends On | Deliverable |
|------|------|------------|-------------|
| **a2-t4-arch-canvas** | Architect canvas simplification | — (START NOW) | Implementation notes: simplified `getCanvasColors()`, MutationObserver deletion plan, white fillRect pattern for A to copy into 6 files |
| **a2-t1-review-tokens** | Review A's token implementation | A completes a-t1-tokens | Sign-off: all 8 values match spec, `--grid-minor` added to both `:root` and `body.arena-mode` |
| **a2-t5-review-final** | Final code review of all A's changes | A completes a-t4a, a-t4b, a-t5 | Sign-off: no regressions, canvas simplified, hex migrated, ready for QA |

**Start now:** `a2-t4-arch-canvas` — write the canvas architecture notes so A can execute.

---

### 🔨 Agent A — Application Engineer

You implement. Follow D's CSS specs and A2's architecture notes.

| Task | What | Depends On | File(s) |
|------|------|------------|---------|
| **a-t1-tokens** | Token refresh: 8 values + 1 new | — (START NOW) | `shared/styles.css` |
| **a-t2-stripe** | Jersey stripe motif | a-t1 + a2-t1-review + d-t2-spec | `shared/styles.css` |
| **a-t3-qnumbers** | Question number restyle | a-t1 + a2-t1-review + d-t2-spec | `shared/styles.css` |
| **a-t4a-canvas-mvp** | Graph canvas fix — MVP exam | a-t1 + a2-t4-arch | `nonlinear_exam_mvp.html` ⚠️ SMALL EDITS ONLY |
| **a-t4b-canvas-other** | Graph canvas fix — 5 other files | a-t4a | 5 legacy HTML files |
| **a-t5-hex-migrate** | Hardcoded hex migration | a-t1 | `parent.html`, `test-builder.html`, `nonlinear_exam_mvp.html` |

**Start now:** `a-t1-tokens` — 8 value swaps + 1 new token in `shared/styles.css :root` + `body.arena-mode`.

**⚠️ Critical:** `nonlinear_exam_mvp.html` is 70KB. Large multiline edits truncate it. Use small, targeted replacements. Verify file size after each edit. Keep `git checkout HEAD --` ready.

**Token values (exact):**

| Token | Current | New |
|-------|---------|-----|
| `--bg-page` | `#F5F5F5` | `#F4F5F9` |
| `--bg-input` | `#FFFFFF` | `#F8F9FC` |
| `--text-primary` | `#333333` | `#1A1F36` |
| `--text-secondary` | `#666666` | `#5E6378` |
| `--border-default` | `#BEC0C2` | `#C8CCD8` |
| `--border-subtle` | `#E8E8E8` | `#E2E5EF` |
| `--shadow-card` | `rgba(0,0,0,0.06)` | `rgba(0,45,98,0.06)` |
| `--bg-highlight` | `#f0f4ff` | `#EEF1FA` |
| **NEW** `--grid-minor` | — | `#EDF0F5` (light) / `#1E2742` (dark) |

---

### 🎨 Agent D — Design

You spec the visual details for A to implement, then update docs after.

| Task | What | Depends On | Deliverable |
|------|------|------------|-------------|
| **d-t2-spec** | CSS spec: stripe motif + question numbers | — (START NOW) | Exact CSS: selectors, values, gradient syntax, hover states |
| **d-t6-designsys** | Bump .design-system.md to v3.0 | a-t2, a-t3, a-t4a done | Updated design system doc |
| **d-t8-template-audit** | Unified template audit | All QA done | Visual consistency report |

**Start now:** `d-t2-spec` — write the exact CSS for the jersey stripe and question number restyle so A can implement T2 and T3.

---

### 🔬 Agent R — Research / Math

| Task | What | Depends On | Deliverable |
|------|------|------------|-------------|
| **r-t4-verify** | Verify canvas changes preserve math accuracy | A completes a-t4a + a-t4b | Confirmation: axes correct, click-to-plot works, scoring unchanged, all questions grade correctly |

**Wait for:** A to finish canvas work, then verify. Color changes should not affect math — this is a safety check.

---

### 🧪 Agent F — QA

| Task | What | Depends On | Deliverable |
|------|------|------------|-------------|
| **f-t7-light** | QA all 19 pages — light mode | A2 final review + R verification | Pass/fail checklist per file |
| **f-t7-dark** | QA all pages — Arena Mode | f-t7-light | Pass/fail dark mode |
| **f-t7-responsive** | Responsive spot-check 375px + 768px | f-t7-light | 6 key pages at mobile + tablet |

**Wait for:** All implementation + reviews complete, then run full QA pass.

---

## Execution Order (Critical Path)

```
NOW (parallel):
  A  → a-t1-tokens (implement)
  A2 → a2-t4-arch-canvas (architect)
  D  → d-t2-spec (spec)

AFTER T1 + A2 REVIEW:
  A  → a-t2-stripe, a-t3-qnumbers (parallel, need d-t2-spec)
  A  → a-t4a-canvas-mvp (needs a2-t4-arch)
  A  → a-t5-hex-migrate

AFTER T4a:
  A  → a-t4b-canvas-other

AFTER ALL A WORK:
  A2 → a2-t5-review-final
  R  → r-t4-verify

AFTER REVIEW + VERIFY:
  F  → f-t7-light → f-t7-dark → f-t7-responsive
  D  → d-t6-designsys

FOLLOW-UP:
  D  → d-t8-template-audit
```

---

## Locked Decisions (Do Not Revisit)

- Light mode is default. Arena Mode (dark) stays.
- Canvas is **always white** — even in Arena Mode ("whiteboard in dark room").
- Dark tokens unchanged except new `--grid-minor: #1E2742`.
- Print styles stay hardcoded — intentional.
- Legacy file hex cleanup is out of scope (token propagation handles 80%).
- Pistons official palette only: Red `#C8102E`, Blue `#1D42BA`, Navy `#002D62`, Chrome `#BEC0C2`, White `#FFFFFF`.

---

**Update your section in `.agent-status.md` as you complete tasks. Small commits. Verify before marking done.**

— Marcus
