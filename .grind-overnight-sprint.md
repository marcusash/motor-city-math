# 🏀 Grind Overnight Sprint — Feb 20 Dispatch

**From Agent GA, per Mash. The team works overnight. No human in the loop.**

---

## MISSION

Kai scored **13/15 (87%)** on Retake Practice #1 today. His retake is this week.
He needs **14/15 (92%)** for SAAS Grade 4. He missed only W2.b questions (parent function + intercepts).

**Deliver by 7am:** Two new practice exams on the dashboard, ready for Kai to take.
1. `retake-practice-6.json` — W2.b intercept drill (extra practice on his weakness)
2. `retake-practice-7.json` — Balanced mock retake (dress rehearsal)

---

## AGENT ASSIGNMENTS + AUTONOMOUS LOOP

### Agent GR (Research) — CRITICAL PATH

**Phase 1: Build retake-practice-6.json (W2.b drill)**
- 15 questions, same schema as practice-1 through 5
- **4 questions on W2.b** (normally 2-3) — focus on intercept calculations:
  - Square root parent + find x-intercept by setting f(x)=0
  - Absolute value parent + find both x-intercepts
  - Quadratic vertex form + find x-intercepts
  - Cube root parent + find x-intercept
- Solution steps MUST walk through: "Set f(x)=0 → solve for x → intercept is (x, 0)"
- Remaining 11 questions: balanced across W2.a, W3, W4, W5 at same difficulty as practice-1
- Commit to repo when done. Push.

**Phase 2: Build retake-practice-7.json (mock retake)**
- 15 questions, SAME distribution as retake-practice-1 (this simulates the real test)
- 2-3 W2.b questions (standard distribution), all different numbers from practice 1-6
- This is Kai's dress rehearsal — it should feel like the actual exam
- Commit and push when done.

**When both are done:** Post in `.agent-status.md`: "GR: practice-6 and practice-7 pushed. Ready for GF verification."

### Agent GF (Fundamentals) — VERIFICATION

**Wait for GR to push each exam, then immediately:**
1. `git pull`
2. Run JSON schema validation (all required fields present)
3. Verify all dropdown answers exist in their options
4. Verify all radio answers exist in their options  
5. Verify graph functions produce correct values at key_points
6. If practice-6 has issues → post in `.agent-status.md` and tell GR what to fix
7. If clean → post "GF: practice-6 verified ✅"
8. Repeat for practice-7

**Fallback if GR hasn't pushed after 30 min:** Run `design-qa.spec.js` regression tests on the simplified site (5 files).

### Agent GI (Data) — ANALYSIS

**Build a mastery report from Kai's 2 exams:**
1. Read `data/kai-scores-2026-02-19.json`
2. Compare MVP (7/15) vs Practice-1 (13/15) — per-standard improvement
3. Identify: which standards are locked (correct on BOTH exams), which improved, which still weak
4. Post the analysis in `.agent-status.md` — this tells GR what to emphasize

**When done:** Post "GI: mastery report posted in .agent-status.md"

### Agent GA (Application) — DASHBOARD UPDATE

**After GR pushes practice-6 and practice-7:**
1. `git pull`
2. Add both exams to `index.html` test registry (same pattern as practice-1 through 5)
3. Validate JSON files load correctly (schema check)
4. Commit and push
5. Post "GA: practice-6 and practice-7 on dashboard ✅"

**If GR hasn't pushed by the time you start:** Pre-wire the registry entries. They'll work as soon as the JSON files are committed.

### Agent GD + GP — STAND BY

No assignments. Site is stable. Only respond if another agent posts a blocker.

---

## THE AUTONOMOUS LOOP

Every agent follows this cycle:

```
1. git pull
2. Check .agent-status.md for updates from other agents
3. Check your inbox (.agent-comms/grind/inbox-{ID}/) for messages
4. Do your assigned work
5. Commit, push
6. Post status in .agent-status.md
7. Wait 10-15 min, repeat from step 1
```

### HANDOFF CHAIN

```
GR builds practice-6 → pushes → GF verifies → GA adds to dashboard
GR builds practice-7 → pushes → GF verifies → GA adds to dashboard
GI works in parallel on mastery report
```

### IF BLOCKED

- GR can't generate questions? Use the 327-question bank (`data/questions.json`) as a source. Filter by standard, change the numbers.
- GF finds math errors? Post the specific error in `.agent-status.md`. GR fixes and re-pushes.
- GA can't add to dashboard? The registry pattern is simple — copy any existing entry and change the file/name/key.

### DEFINITION OF DONE

✅ `data/retake-practice-6.json` exists and passes schema validation
✅ `data/retake-practice-7.json` exists and passes schema validation
✅ Both appear in `index.html` retake grid
✅ `exam.html?file=retake-practice-6` renders all 15 questions
✅ `exam.html?file=retake-practice-7` renders all 15 questions
✅ Correct answers score 15/15 on both
✅ W2.b solution steps explain intercept calculation clearly

---

## KAI'S SCORE CONTEXT

**MVP Exam (Feb 18):** 7/15 (47%) — Grade 1
- A: 2/3, B: 5/8, C: 0/2, D: 0/2

**Retake Practice #1 (Feb 19):** 13/15 (87%) — Grade 3
- A: 1/3, B: 8/8, C: 2/2, D: 2/2
- Wrong: Q2 (W2.b sqrt intercepts), Q3 (W2.b abs value intercepts)

**Pattern:** Kai knows parent functions. He struggles solving f(x)=0 when transformations are involved. The solution steps need to explicitly show: "Set the whole function equal to zero, then isolate x."

---

**Ship it. Kai practices in the morning. Every hour we delay costs him a practice cycle.**
