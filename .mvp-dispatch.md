# 📋 MVP SPEC READY — All Agents Read `.mvp-spec.md`

**From:** Marcus via Agent D
**Date:** 2026-02-17
**Priority:** URGENT — Kai needs this test today

---

## What

Single HTML file `nonlinear_exam_mvp.html` — 15-question nonlinear functions exam (Unit 2, W2 & W3 standards). ALL new numbers — Kai has seen the source tests.

**Spec file:** `.mvp-spec.md` in repo root. Read the whole thing.

---

## Agent Assignments

### 🔬 Agent R — FIRST PRIORITY

- Double-verify all 15 answers (Section "⚠️ MATH VERIFICATION REQUIRED")
- Verify Q12/Q13 graph functions produce clean values at 0.25 snap points
- Write hints + solution_steps for all 15 questions (per `.hint-system-spec.md` format)
- Flag any math errors BEFORE Agent A starts building

### 🏗️ Agent A — AFTER R verifies

- Build `nonlinear_exam_mvp.html` as a single file using `shared/styles.css` + `shared/scripts.js`
- Extend `gradeTest()` to store per-question results with type/standard metadata (see Score Storage section in spec)
- Copy canvas graphing from `nonlinear_functions_test.html`, add: coordinate tooltip (snap 0.25), point-check algorithm, min 5 points required
- Parent function dropdowns (not text input), labeled number inputs for ± answers, multiple choice for Q14
- SAAS 4-point grade display (≥92=4, ≥82=3, ≥70=2, <70=1)

### 🧪 Agent F — IN PARALLEL

- Build e2e test suite for graphing (21 test cases spec'd in the doc)
- Test point-check algorithm, asymptote validation, tooltip snap, canvas sizing
- Test score storage: attempt append, per-question tracking, best-score update, FIFO at 20

### 🎨 Agent D — DONE

- Spec is complete and reviewed. Standing by for visual QA once A has a build.

---

## Key Design Decisions (already made)

- **No special characters needed** — dropdowns, decimals, multiple choice
- **Graph tolerance:** ±0.25 grid units
- **Grid coordinate tooltip** on mousemove (snap to 0.25)
- **Wrong graph points** trigger 3-layer hint system
- **Score storage:** full attempt history + per-question type/standard tracking
- **Canvas:** 700×700 desktop, min 500×500 mobile
- **SAAS grading:** 4-point scale (≥92=4, ≥82=3, ≥70=2, <70=1)

---

## Rules

- **Don't touch** existing test files. One file at a time. Kai is actively studying.
- **Math accuracy is non-negotiable.** R verifies before A builds.
- **Read the full spec** before starting. It has HTML snippets, pseudocode, e2e test cases, and storage schemas.

— Marcus
